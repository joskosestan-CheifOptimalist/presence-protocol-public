name: ci

on:
  pull_request:
  push:
    branches: [ main ]

jobs:
  lint-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Spell check docs (placeholder)
        run: echo "TODO: add markdown lint" 

  mobile-sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Gradle wrapper exists
        run: test -f gradlew || echo "TODO: add Android build once module exists"
